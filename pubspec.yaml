name: svs
description: "Streams and renders Aperio SVS whole-slide pathology images: pan/zoom, LOD tile streaming, JPEG & JPEG2000, no full-image load."
version: 1.3.0
homepage: https://github.com/kiu-chan/svs
repository: https://github.com/kiu-chan/svs
issue_tracker: https://github.com/kiu-chan/svs/issues
funding:
  - https://ko-fi.com/monlycute
topics:
  - image
  - medical
  - widget

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  archive: ^4.0.9
  image: ^4.9.2
  openjpeg_ffi: ^0.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
