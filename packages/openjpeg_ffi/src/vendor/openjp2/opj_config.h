/* Hand-resolved from opj_config.h.cmake.in for OpenJPEG 2.5.4 — this
 * package builds openjp2 directly via each platform's normal C toolchain
 * (see hook/build.dart) rather than OpenJPEG's own CMake, so there is no
 * configure step to generate this file. Version numbers pinned to match
 * the vendored source tree; keep in sync if the vendored version changes. */
#ifndef OPJ_CONFIG_H_INCLUDED
#define OPJ_CONFIG_H_INCLUDED

#define OPJ_VERSION_MAJOR 2
#define OPJ_VERSION_MINOR 5
#define OPJ_VERSION_BUILD 4

#endif
