## 0.0.1

* Initial release: `decodeJ2k` for decoding raw JPEG2000 codestreams via a vendored OpenJPEG 2.5.4, built through `hook/build.dart` (native-assets) for macOS/iOS/Android; Windows/Linux untested locally, expected to work unchanged.
